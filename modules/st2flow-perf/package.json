{
  "name": "@stackstorm/st2flow-perf",
  "version": "1.0.0-pre.5",
  "description": "A small performance library using the performance API and some extra smartness",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "devDependencies": {
    "babelify": "^10.0.0"
  },
  "dependencies": {
    "debug": "^4.0.1"
  }
}
