{
  "name": "@stackstorm/st2flow-yaml",
  "version": "1.0.0-pre.5",
  "description": "",
  "private": true,
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "devDependencies": {
    "babelify": "^10.0.0"
  },
  "dependencies": {
    "@stackstorm/st2flow-perf": "1.0.0-pre.5",
    "yaml-ast-parser": "0.0.43"
  }
}
